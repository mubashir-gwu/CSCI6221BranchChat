Audit cycle 2 found 1 medium issue that needs fixing:

1. **Tests not updated for parentNodeId validation fix — 8 tests fail**
   - File: `__tests__/api/llm-chat.test.ts`
   - Issue: The default `mockNodeFind` in `beforeEach` (line 123-125) returns an empty array via `.lean()`. All tests using `validBody` (which has `parentNodeId: "node-1"`) now hit the new parentNodeId validation at `route.ts:112-118` and get a 400 response before reaching the LLM call or node creation logic.
   - Fix: Update the default `mockNodeFind` mock in `beforeEach` to return at least one node with `_id` matching "node-1" so the parentNodeId validation passes:
     ```typescript
     mockNodeFind.mockReturnValue({
       lean: () => [
         {
           _id: { toString: () => "node-1" },
           conversationId: { toString: () => "conv-1" },
           parentId: null,
           role: "user",
           content: "Previous message",
           provider: null,
           model: null,
           createdAt: now,
         },
       ],
     });
     ```
     This change affects all 8 failing tests. No other test changes should be needed — the `parentNodeId: null` tests (like "should set rootNodeId when parentNodeId is null") already pass because `null` skips the validation check.
