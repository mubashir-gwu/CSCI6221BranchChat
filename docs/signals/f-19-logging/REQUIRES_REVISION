1. File: `src/lib/logger.ts` — The async fire-and-forget `writeLog` calls race against each other, causing non-deterministic write ordering. Add a write queue that serializes appends: each `writeLog` call should chain onto the previous promise so writes complete in call order. For example, maintain a module-level `let writeChain = Promise.resolve()` and in `writeLog`, do `writeChain = writeChain.then(() => doWrite(...))`. This preserves fire-and-forget semantics at call sites while guaranteeing ordering. The test `supports all log levels` in `__tests__/lib/logger.test.ts` currently fails due to this — it should pass after the fix without test changes.
