1. File: src/hooks/useTreeLayout.ts, lines 64-72
   Issue: The rfEdges loop does not have the same nodes.has() guard that was added to the Dagre edge loop at line 36. Both loops iterate childrenMap, but only the first guards against parentId/childId missing from the nodes map.
   Fix: Add the same guard to the rfEdges loop. Change:
     ```
     for (const childId of children) {
       rfEdges.push({
     ```
   to:
     ```
     for (const childId of children) {
       if (!nodes.has(parentId) || !nodes.has(childId)) continue;
       rfEdges.push({
     ```
